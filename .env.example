# No API keys required for ONS (dados.ons.org.br) or IPEADATA (CCEE PLD).
# Open-Meteo weather is also free without a key.

# Optional: override default data paths
# DATA_DIR=/path/to/custom/data/dir
